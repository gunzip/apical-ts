---
"@apical-ts/website": minor
"@apical-ts/craft": minor
---

Add `--routes` CLI flag to generate only schemas and route metadata

This new flag allows developers to generate type-safe schemas and route metadata
without the full client or server implementations, enabling custom client/server
implementations using preferred frameworks or tools.
